# =====================================================================
# Building the reference database (create_fingerprint.py)
# =====================================================================
# Core phonetic fingerprinting
metaphone>=0.6
python-Levenshtein>=0.21

# Subtitle parsing
pysrt>=1.1.2
webvtt-py>=0.4.6

# OpenSubtitles download
requests>=2.28

# =====================================================================
# Identification (identify_dvd_episodes.py)
# =====================================================================
# Identification is purely dialogue-based (phonetic). Speech-to-text
# transcribes a few short audio samples from each unknown file, which are
# then matched against the subtitle-built reference database. The
# database-building script does NOT need these - skip them if you only
# build/index reference subtitles.
vosk>=0.3.45              # offline STT (recommended) - requires a model download
SpeechRecognition>=3.10  # optional online Google Web Speech engine

# Audio handling for STT loading/transcription (requires system ffmpeg).
# ffmpeg/ffprobe are also used to extract the sampled audio windows.
pydub>=0.25.1

# =====================================================================
# Modern desktop GUI (dvd_identifier_fluent.py)
# =====================================================================
# Windows 11 Fluent Design front-end over the identification engine.
# Installing PySide6-Fluent-Widgets pulls in PySide6 automatically.
PySide6-Fluent-Widgets>=1.11.0
PySide6>=6.6
